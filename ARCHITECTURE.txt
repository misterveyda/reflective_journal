╔═══════════════════════════════════════════════════════════════════════════╗
║                  REFLECTIVE JOURNAL - ARCHITECTURE                         ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                          CLIENT BROWSER                                 │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  FRONTEND (Port 8001)                                            │  │
│  │  ━━━━━━━━━━━━━━━━━━━━━━                                          │  │
│  │  • index.html  - Single HTML file                                │  │
│  │  • app.js      - All JavaScript logic (~350 lines)              │  │
│  │  • styles.css  - Complete styling                               │  │
│  │                                                                  │  │
│  │  Login → Create Entry → View Entries                            │  │
│  │  Mood emojis: 😊 😢 😐 😰 🤩                                     │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                               │
                    ┌──────────┴──────────┐
                    │ HTTP/JSON over REST │
                    └──────────┬──────────┘
                               │
┌─────────────────────────────────────────────────────────────────────────┐
│                        SERVER (Port 8000)                               │
│                                                                         │
│  DJANGO REST API                                                        │
│  ━━━━━━━━━━━━━━━                                                        │
│                                                                         │
│  ┌─────────────────────────────────┐                                   │
│  │ config/                         │                                   │
│  │ • settings.py - All config     │                                   │
│  │ • urls.py     - Route /api/*   │                                   │
│  │ • wsgi.py     - WSGI app       │                                   │
│  └─────────────────────────────────┘                                   │
│                                                                         │
│  ┌─────────────────────────────────┐                                   │
│  │ journal/                        │                                   │
│  │ • models.py    - JournalEntry  │                                   │
│  │ • views.py     - ViewSet       │                                   │
│  │ • serializers  - JSON format   │                                   │
│  │ • urls.py      - /api/entries/ │                                   │
│  │ • admin.py     - Admin panel   │                                   │
│  └─────────────────────────────────┘                                   │
│                                                                         │
│  REST ENDPOINTS:                                                        │
│  • POST   /api/auth/login/         → Returns token                    │
│  • POST   /api/auth/registration/  → Returns token                    │
│  • GET    /api/entries/            → List entries                     │
│  • POST   /api/entries/            → Create entry                     │
│  • GET    /api/entries/{id}/       → Get entry                        │
│  • PUT    /api/entries/{id}/       → Update entry                     │
│  • DELETE /api/entries/{id}/       → Delete entry                     │
└─────────────────────────────────────────────────────────────────────────┘
                               │
                    ┌──────────┴──────────┐
                    │  SQLite Database   │
                    │  (db.sqlite3)      │
                    └────────────────────┘


╔═══════════════════════════════════════════════════════════════════════════╗
║                         AUTHENTICATION FLOW                                ║
╚═══════════════════════════════════════════════════════════════════════════╝

1. USER SIGNUP:
   Frontend: POST /api/auth/registration/ {email, password}
   Backend:  Create User + Token
   Return:   {key: "abc123xyz..."}
   Frontend: localStorage.setItem('token', key)

2. USER LOGIN:
   Frontend: POST /api/auth/login/ {email, password}
   Backend:  Verify credentials, return token
   Return:   {key: "abc123xyz..."}
   Frontend: localStorage.setItem('token', key)

3. AUTHENTICATED REQUEST:
   Frontend: GET /api/entries/
            Headers: {Authorization: "Token abc123xyz..."}
   Backend:  Verify token, return user's entries
   Return:   [{id, title, content, mood, created_at}, ...]


╔═══════════════════════════════════════════════════════════════════════════╗
║                           FOLDER STRUCTURE                                 ║
╚═══════════════════════════════════════════════════════════════════════════╝

reflective_journal/
│
├── backend/
│   ├── config/
│   │   ├── __init__.py
│   │   ├── settings.py      ← Django config
│   │   ├── urls.py          ← URL routing
│   │   └── wsgi.py          ← WSGI application
│   │
│   ├── journal/
│   │   ├── __init__.py
│   │   ├── models.py        ← Database models
│   │   ├── views.py         ← API views (ViewSet)
│   │   ├── serializers.py   ← JSON serializers
│   │   ├── urls.py          ← Journal URLs
│   │   ├── apps.py
│   │   ├── admin.py         ← Admin panel config
│   │   └── migrations/      ← Database migrations
│   │
│   ├── manage.py
│   ├── requirements.txt     ← Python dependencies
│   ├── .env.example
│   └── db.sqlite3           ← Database (auto-created)
│
├── frontend/
│   ├── index.html           ← Main HTML
│   ├── app.js               ← All JavaScript
│   └── styles.css           ← All CSS
│
├── README.md                ← Project overview
├── SETUP.md                 ← Full setup guide
├── FRESH_START.txt          ← What changed
├── setup.sh                 ← Setup script
└── .git/                    ← Git repository


╔═══════════════════════════════════════════════════════════════════════════╗
║                           QUICK COMMANDS                                   ║
╚═══════════════════════════════════════════════════════════════════════════╝

BACKEND:
  cd backend
  python3 -m venv .venv
  source .venv/bin/activate          # Windows: .venv\Scripts\activate
  pip install -r requirements.txt
  python manage.py migrate
  python manage.py createsuperuser
  python manage.py runserver         # Runs on http://localhost:8000

FRONTEND:
  cd frontend
  python -m http.server 8001         # Runs on http://localhost:8001

ADMIN PANEL:
  http://localhost:8000/admin/


╔═══════════════════════════════════════════════════════════════════════════╗
║                        KEY TECHNOLOGIES                                    ║
╚═══════════════════════════════════════════════════════════════════════════╝

BACKEND:
  • Django 4.2.9           - Web framework
  • djangorestframework     - API
  • dj-rest-auth          - Authentication
  • django-allauth        - User management
  • django-cors-headers   - CORS support
  • SQLite3               - Database

FRONTEND:
  • HTML5                 - Markup
  • CSS3                  - Styling
  • Vanilla JavaScript    - Logic (no frameworks!)
  • Fetch API             - HTTP requests
  • localStorage          - Session storage

WHY THIS STACK:
  ✓ Simple - No React, no npm nightmare
  ✓ Clean - Clear separation of concerns
  ✓ Fast - Lightweight and responsive
  ✓ Secure - Token-based auth
  ✓ Local - SQLite database
  ✓ Deployable - Works on any server


═══════════════════════════════════════════════════════════════════════════════
